# ui

React + Vite + TypeScript + Tailwind frontend for the agent-sandbox dashboard.

- `npm run dev` — start the Vite dev server on :5173 (proxies `/api` to `:8080`).
- `npm run build` — produce `dist/` for the Go binary to embed.

See the repo-level README for end-to-end build instructions.
