# GENERATED FILE — DO NOT EDIT.
#
# Rendered from deploy/helm/sandbox-dashboard (chart 0.4.0) with default
# values, into the "default" namespace. Regenerate with:
#
#     make manifests
#
# This is the no-Helm install path:
#
#     kubectl apply -f https://raw.githubusercontent.com/aGallea/sandbox-dashboard/main/deploy/install.yaml
#
# It deploys with both optional integrations off: the metrics page and the
# overview's usage panels report that Prometheus is unconfigured, and sandbox
# rows carry no OpenSandbox state. To turn either on, use the chart, or patch
# this file with kustomize:
#
#     resources: [install.yaml]
#     patches: [...]
---
# Source: sandbox-dashboard/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sandbox-dashboard
  labels:
    helm.sh/chart: sandbox-dashboard-0.4.0
    app.kubernetes.io/name: sandbox-dashboard
    app.kubernetes.io/instance: sandbox-dashboard
    app.kubernetes.io/version: "0.4.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: dashboard
---
# Source: sandbox-dashboard/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  # Cluster-scoped, so the name carries the release to keep two installs apart.
  name: sandbox-dashboard
  labels:
    helm.sh/chart: sandbox-dashboard-0.4.0
    app.kubernetes.io/name: sandbox-dashboard
    app.kubernetes.io/instance: sandbox-dashboard
    app.kubernetes.io/version: "0.4.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: dashboard
rules:
  # Read-only throughout: the dashboard never writes, and has no verbs to.
  - apiGroups: ["agents.x-k8s.io"]
    resources: ["sandboxes"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["extensions.agents.x-k8s.io"]
    resources: ["sandboxtemplates", "sandboxclaims", "sandboxwarmpools"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["pods", "events"]
    verbs: ["get", "list", "watch"]
---
# Source: sandbox-dashboard/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: sandbox-dashboard
  labels:
    helm.sh/chart: sandbox-dashboard-0.4.0
    app.kubernetes.io/name: sandbox-dashboard
    app.kubernetes.io/instance: sandbox-dashboard
    app.kubernetes.io/version: "0.4.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: dashboard
subjects:
  - kind: ServiceAccount
    name: sandbox-dashboard
    namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: sandbox-dashboard
---
# Source: sandbox-dashboard/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: sandbox-dashboard
  labels:
    helm.sh/chart: sandbox-dashboard-0.4.0
    app.kubernetes.io/name: sandbox-dashboard
    app.kubernetes.io/instance: sandbox-dashboard
    app.kubernetes.io/version: "0.4.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sandbox-dashboard
    app.kubernetes.io/instance: sandbox-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
---
# Source: sandbox-dashboard/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sandbox-dashboard
  labels:
    helm.sh/chart: sandbox-dashboard-0.4.0
    app.kubernetes.io/name: sandbox-dashboard
    app.kubernetes.io/instance: sandbox-dashboard
    app.kubernetes.io/version: "0.4.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: sandbox-dashboard
      app.kubernetes.io/instance: sandbox-dashboard
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sandbox-dashboard
        app.kubernetes.io/instance: sandbox-dashboard
      annotations:
    spec:
      serviceAccountName: sandbox-dashboard
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: dashboard
          image: "ghcr.io/agallea/sandbox-dashboard:0.4.0"
          imagePullPolicy: IfNotPresent
          env:
            # The port comes from the environment rather than args so that one
            # value drives the listener, the container port and the probes.
            - name: AGENT_SANDBOX_DASHBOARD_LISTEN_ADDR
              value: "8080"
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            # /readyz stays 503 until every informer has finished its first sync,
            # so traffic never reaches a pod that would serve an empty cluster.
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
