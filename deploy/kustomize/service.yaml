apiVersion: v1
kind: Service
metadata:
  name: agent-sandbox-dashboard
  labels:
    app.kubernetes.io/name: agent-sandbox-dashboard
    app.kubernetes.io/component: dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-sandbox-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
